---
description: Trigger the Multi-Repo Orchestrator to synchronize architectural changes across microservices/monorepos
argument-hint: [instruction]
allowed-tools: Read, Grep, Glob, Bash
---

Invoke the `multi-repo-orchestrator` subagent to coordinate changes across multiple repositories in the workspace.
