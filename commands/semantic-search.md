---
description: Search code by meaning (semantic) — not just regex
argument-hint: [query]
allowed-tools: Read, Grep, Glob, Bash
---
Invoke the `Explore` agent for semantic code search. Run `coder-workflow semantic-search <query>` or MCP tool `semantic_search`. Build embeddings first: `coder-workflow semantic-search --build`.
