---
description: Detect unused exports, orphan files, and uncalled functions using CodeGraph
allowed-tools: Read, Grep, Glob, Bash, mcp__codegraph__*
---
Invoke the `architecture-auditor` subagent to find dead code. Run `coder-workflow dead-code` or use MCP tool `find_dead_code`.
